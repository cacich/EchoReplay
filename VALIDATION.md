# 驗證紀錄

## 1.2.0 程式分軌驗證（2026-09-16）

- 37 項自動化測試通過，新增版本門檻、名稱正規化、Electron 程序樹／多根／重啟解析、來源重疊與自身程序樹保護、三軌混音逐取樣一致，以及舊設定／進度相容性。
- Windows 11 build 26100 實際以兩個獨立程序播放 880 Hz／1320 Hz 測試音，使用 Windows process loopback 分開錄製。目標頻率振幅約 0.035，另一來源的頻率成分低於 5% 驗收門檻。
- 排除指定程序時仍能取得另一來源；排除自身程序樹時沒有自身 440 Hz 測試訊號。
- 底層擷取在來源退出、新 PID 啟動後成功重連。
- 完整 `ReplayEngine` 以真實 .exe 名稱解析兩個兄弟程序，成功保存語音／遊戲獨立 WAV、混音及來源資訊，剪輯器能載入。即使設定原先未勾選分軌，程序模式仍保存來源音軌。
- 語音來源退出後以同名新程序重啟，能重新錄到聲音，遊戲來源 PID 維持不變。
- 所有程式模式保留其他來源的測試音，自身 440 Hz 成分約 0.000000194；指定裝置相容模式仍錄得到自身測試音。
- 三軌 WPF 剪輯診斷通過保存／重開、音量、匯出及回收筒清理；設定與三軌編輯畫面已檢查。

尚未實測：真實 Discord／遊戲各版本、反作弊／受保護程序、跨實體裝置切換、麥克風與程序音訊的長時間同步。合成訊號測試不代表這些情境均已驗證。

## 1.1.0 剪輯驗證（2026-09-16）

- 31 項自動化測試通過，包含原有 13 項錄音／設定測試及 18 項剪輯、編碼與檔案處理測試。
- 以逐取樣比對檢查裁切、連續多段刪除、靜音、分軌、淡入淡出與復原／重做；WAV 匯出與試聽串流的 PCM 完全一致。
- Windows Media Foundation 實際輸出並重新解碼 192／320 kbps MP3，檢查聲道、時長、位元率及非零訊號。
- 驗證保存／重新載入進度、來源變更拒絕舊進度、取消保護、防覆寫來源、匯入副本、改名、刪除範圍及未知檔案保護。
- 已執行 WPF 剪輯診斷：最後 5 秒、裁切、接合、靜音、復原／重做、音量／淡化、保存／重開、WAV 匯出與試聽串流前進均通過。
- 實際把診斷建立的匯入錄音群組移至 Windows 資源回收筒，確認該群組消失，獨立匯出成品與其他錄音仍存在。
- 在編輯計算時並行寫入錄音循環緩衝的測試通過。這不代表整晚錄音、遊戲負載或不同硬體的長時間驗證。
- 另外在開啟 WASAPI 電腦錄音時執行整段剪輯介面診斷與短暫試聽，確認錄音時間與擷取封包持續增加（診斷 `CaptureContinued: true`）。
- MP3 使用有損編碼，容許編碼延遲／padding 帶來的短暫長度差；WAV 採取樣精確的長度驗證。

以下為原有錄音功能的驗證紀錄與仍適用的硬體限制。

日期：2026-09-15，Windows x64。

## 自動化測試

13 項通過：循環覆蓋、靜音缺口、起始負時間戳、延遲封包、雙來源時間對齊、快照獨立性、無效取樣清理、WAV 格式／長度／取樣、混音與限幅、快捷鍵解析、Windows 快捷鍵占用及恢復、設定儲存與損壞復原，以及並行讀寫。

## 實機驗證

- Realtek 播放裝置成功透過 WASAPI loopback 錄得測試音；WAV 具有非零音訊取樣，格式為 48 kHz、16-bit、雙聲道。
- 隱藏視窗後，緩衝時間持續增加並可存檔。
- 輸出路徑不可寫入時回報失敗，錄音與緩衝繼續保留。
- 暫停後緩衝時間固定，仍可匯出；重新開始會建立新緩衝。
- 已透過介面的設定套用流程，驗證修改輸出路徑及混音音量後寫入設定檔，並保留正在錄製的緩衝。
- 原生 Windows 熱鍵註冊的衝突及恢復，已由自動化測試實際呼叫 Windows API 驗證。
- 主畫面及設定頁已透過 WPF 畫面輸出檢查。

## 尚未在此環境完成的驗證

- 目前沒有可用的預設麥克風，Windows 回報 `0x80070490`。麥克風的實際收音品質及與電腦聲音的實機同步，需接上麥克風後測試；程式已呈現中斷狀態並定期重試。
- 沒有實際重新登入 Windows；開機啟動的使用者登錄項目由使用者在設定中啟用。
- 沒有在遊戲／Discord 中實測快捷鍵，亦未測試耳機實體拔插、驅動更新或整晚錄音。

短時間驗證不代表所有音效驅動與長時間使用情境均已驗證。

## 專案文件與建置交付

- 已檢查 8 份 Markdown 文件、25 個本機文件與圖片連結。
- 依 README 的 `build.ps1 -Portable` 流程重新建置；13 項自動化測試通過，產出 Windows x64 可攜版。
- 待提交內容僅包含原始碼、圖示、文件、建置腳本與測試；SDK、編譯產物、錄音、個人設定及 SSH 金鑰不納入 Git。
