# 貢獻與維護說明

## 修改專案

1. 閱讀 [製作說明](docs/DEVELOPMENT.md) 與 [架構說明](docs/ARCHITECTURE.md)。
2. 建立功能分支，讓每次修改有明確範圍。
3. 涉及錄音、時間軸、WAV 或快捷鍵時執行對應測試；涉及裝置與介面時，再做相關實機驗證。
4. 更新文件與 `CHANGELOG.md`。驗證紀錄應區分已完成、未完成及僅以合成資料測試的項目。
5. 提交前以 `git diff --cached --stat` 檢查內容，避免提交音檔、個人設定、金鑰或建置產物。

```powershell
dotnet run --project .\tests\EchoReplay.Tests\EchoReplay.Tests.csproj -c Release
```

Pull Request 請簡述問題、修改後行為、測試結果及仍待驗證的情境。

## 回報問題

提供程式版本、Windows 版本、音效裝置、設定與重現步驟。無聲或不同步問題請說明是哪一個來源，以及混音或分軌是否也出現相同問題。

先參考 [疑難排解](docs/TROUBLESHOOTING.md)，排除裝置選擇、Windows 麥克風權限及快捷鍵占用等常見原因。

## 專案慣例

- 介面與使用文件採用繁體中文。
- 保持擷取、緩衝、匯出與介面責任清楚，不在音訊擷取執行緒操作 WPF 控制項。
- 匯出失敗不能清除仍在使用的緩衝；暫存檔清理只能處理該次匯出建立的目錄。
- 使用者的路徑、啟動偏好與快捷鍵由設定決定。
- 更新第三方套件時，同步檢查授權聲明與打包結果。
